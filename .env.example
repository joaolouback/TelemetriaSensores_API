PORT=3000
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/uvvgo?schema=public"

# Segredo usado para assinar os tokens de login (JWT). Troque por um valor longo e aleatório.
JWT_SECRET="troque-este-segredo"
